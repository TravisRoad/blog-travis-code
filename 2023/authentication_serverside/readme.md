# authentication on server

```shell
# run server code
{
    cd backend
    go mod tidy
    go run main.go
    # run on :8080
}

# run client code

{
    pnpm i # or other package manager you like
    pnpm dev
    # default run on :3000
    # open http://localhost:3000 in browser
}
```
